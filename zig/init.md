# Init

เริ่มต้นด้วย

```
zig init
```

zig จะทำการสร้าง file มาให้ตามนี้

```
created build.zig
created build.zig.zon
created src/main.zig
created src/root.zig
```

โดยแต่ละ file จะใช้งานในหน้าที่ต่างกันดังนี้
